Demo script to set up sample database, schema, tables  with sample records. 
Contain different examples with JSON payloads to push external lineage events into snowflake using open lineage standards.

This script also includes clean up commands if needed.
